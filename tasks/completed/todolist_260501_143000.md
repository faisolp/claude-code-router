# Task: Fix DeepSeek reasoning_content fallback
_Created: 2026-05-01 14:30:00_

## Scope
- แก้ไข fallback logic ใน deepseek.transformer.ts
- Ensure ทุก assistant message มี reasoning_content เสมอ แม้ไม่มี cache

## Todo
- [x] แก้ไข fallback logic inject empty string ถ้าไม่มี cached reasoning
- [x] Syntax check TypeScript

## Files Affected
- `packages/core/src/transformer/deepseek.transformer.ts`

## Notes
- DeepSeek V4 Pro ต้องการ reasoning_content ในทุก assistant messages แม้จะเป็น empty string
- Error: "The `reasoning_content` in the thinking mode must be passed back to the API."

## Result
_Completed: 2026-05-01 14:35:00_
แก้ไข fallback logic ใน deepseek.transformer.ts:
- เอาเงื่อนไข `this.currentSessionId &&` ออกเพื่อให้ทำงานแม้ไม่มี session
- เปลี่ยนจาก `if (cachedReasoning && ...)` เป็น `if (this.currentIsV4Pro && ...)`
- Inject `cachedReasoning || ''` แทนที่จะ inject เฉพาะกรณีมี cache
- ทำให้ทุก assistant message มี reasoning_content เสมอ (empty string เมื่อไม่มี cache)
